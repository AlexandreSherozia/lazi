{% extends 'base.html.twig' %}
{% block title %}
    {{ parent() }} initialize password
{% endblock %}

{% block content %}
    <div class="m-grid m-grid--hor m-grid--root m-page" id="ffcb_login_form">
        <div class="m-grid__item m-grid__item--fluid m-grid m-grid--hor m-login m-login--signin m-login--2 m-login-2--skin-2" id="m_login" style="background-image: url(../../../assets/app/media/img//bg/bg-3.jpg);">
            <div class="m-grid__item m-grid__item--fluid	m-login__wrapper">
                <div class="m-login__container">
                    <div class="m-login__logo">
                        <a href="javascript:">
                            <img style="width: 85px;" src="{{ asset('lazi_logos/lazi_logo_alpha.png') }}">
                        </a>
                    </div>
                    <div class="m-login__signin">
                        <div class="m-login__head">
                            <h3 class="m-login__title">Pour recevoir le lien de réinitialisation, <br>entrez votre email</h3>
                        </div>
                        <form class="m-login__form m-form" method="post" action="{{ path('send_email_for_password_initializer') }}">

                            {#{% if error %}
                                <div class="alert alert-danger text-center">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
                            {% endif %}
#}
                            <div class="form-group m-form__group">
                                <input class="form-control m-input"  type="email" placeholder="Email" name="email" autocomplete="off">
                            </div>


                            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

                            <div class="m-login__form-action">
                                <input name="submit_button" type="submit" value="M'envoyer email" class="btn btn-focus m-btn m-btn--pill m-btn--custom m-btn--air m-login__btn m-login__btn--primary">
                            </div>
                        </form>
                    </div>

                </div>
            </div>
        </div>
    </div>
{% endblock %}


