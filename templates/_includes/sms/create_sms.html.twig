{{ form_start(smsForm, {'attr': {class: 'm-form m-form--fit m-form--label-align-right', id: 'smsForm', style: 'padding-bottom:40px'}}) }}
<div class="row">
    <div class="offset-4 col-lg-3">
        <h3 style="color: #a7a7a7;">{{ "sms ffcb"|upper }}</h3>
    </div>

    {{ form_widget(smsForm.isDraft) }}

    <button type="submit" class="btn btn-default offset-1" style="width: 70px; height: 40px;background-color: #ffbc42">
        <span class="flaticon-paper-plane"></span>
    </button>
</div>

<div class="m-form__seperator m-form__seperator--dashed m-form__seperator--space-2x"
     style="margin: 30px 3px"></div>

<div class="form-group m-form__group row">
    <div class="col-lg-12">
        <div class="row">
            <label for="example-text-input" class="col-lg-2 col-form-label"
                   style="color: #ff5e6d">{{ 'à'|upper }} :</label>
            <div class="col-lg-10">
                <span class="red">{{ form_errors(smsForm.recipientTo) }}</span>
                {{ form_widget(smsForm.recipientTo, {
                    attr: {
                        class: 'form-control',
                        style: 'border: 1px solid grey',
                    }
                }) }}
            </div>
        </div>
    </div>
</div>

<div class="m-form__seperator m-form__seperator--dashed m-form__seperator--space-2x"
     style="margin: 30px 3px"></div>


<div class="form-group m-form__group row">
    <div class="col-lg-12">
        <div class="row">

            <label for="example-text-input" class="col-lg-2 col-form-label"
                   style="color: #ff5e6d">Message :</label>
            <div class="col-lg-10">
                <span class="red">{{ form_errors(smsForm.message) }}</span>
                {{ form_widget(smsForm.message, {
                    attr: {
                        class: 'form-control m-input',
                        style: 'border: 1px solid grey; height: 100px',
                    }
                }) }}
            </div>
        </div>
    </div>
</div>

{{ form_widget(smsForm._token) }}
{{ form_end(smsForm, {'render_rest': false }) }}

